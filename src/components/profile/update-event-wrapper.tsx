interface UpdateEventWrapperProps {
  eventId: string;
}

const UpdateEventWrapper = ({ eventId }: UpdateEventWrapperProps) => {
    // fetch event
    // show error
    // not found
    // return form
  return <></>;
};

export default UpdateEventWrapper;
